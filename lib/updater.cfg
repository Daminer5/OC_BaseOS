-- OC_BaseOS updater configuration
-- Set which GitHub branch to sync from.
-- Switch between "main" and "dev" during development.
return {
  repo = "https://raw.githubusercontent.com/Daminer5/OC_BaseOS/refs/heads",
  branch = "dev"  -- default branch for current development
}
